/*global dessert, troop, node */
(function () {
    "use strict";

    /**
     * @class node.http
     * @extends troop.Base
     */
    node.http = troop.Base.extend()
        .addMethods(/** @lends node.http*/{
        });
}());
