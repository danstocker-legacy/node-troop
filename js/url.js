/*global dessert, troop, node */
(function () {
    "use strict";

    /**
     * @class node.url
     * @extends troop.Base
     */
    node.url = troop.Base.extend()
        .addMethods(/** @lends node.url*/{
        });
}());
