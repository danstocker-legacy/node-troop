/*global dessert, troop, node */
(function () {
    "use strict";

    /**
     * @class node.os
     * @extends troop.Base
     */
    node.os = troop.Base.extend()
        .addMethods(/** @lends node.os*/{
        });
}());
