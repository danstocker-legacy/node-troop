/*global dessert, troop, node */
(function () {
    "use strict";

    /**
     * @class node.crypto
     * @extends troop.Base
     */
    node.crypto = troop.Base.extend()
        .addMethods(/** @lends node.crypto*/{
        });
}());
